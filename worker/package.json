{
  "name": "inbox-email-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Email Worker that stores incoming mail into KV for the Inbox Viewer app.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "postal-mime": "^2.3.2"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
